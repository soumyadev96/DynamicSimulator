#pragma once
//Reads the Scheduling parameter from the network administrator (Here simulation parameter)
//Use the Scheduling parameterto ready the Decision equation for selection criteria of traffic requests.
//Finally ready the traffic image for one-go (we can say unit time) routing.