#pragma once
//Reads the Network Topology time to time to keep track of the original knowledge graph
//Generate classified (UGS/BE/NrTPS etc.) Traffic request concurrently according to the user request(in this case randomly)
//Maintain Different queue for different network node and treat the dynamic traffic accordingly( like updating of traffic request’s session)
#include "NetworkTopology.h"
